var gameScore = 0;
var SCORE_ROW_FULL = 100;      // BONUS score if a row becomes full
var SCORE_BLOCK_LANDED = 25;   // score if a block lands
var SCORE_BLOCK_DROP = 5;      // score everytime a block drops a block
var SCORE_BLOCK_DROPFAST = 10; // BONUS score if user initiates dropfast
var scoreArea = document.getElementById("score");
